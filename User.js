import mongoose from 'mongoose'; const UserSchema = new mongoose.Schema({ name:String, email:{type:String,unique:true,required:true}, phone:String, passwordHash:String, role:{type:String,enum:['citizen','authority','engineer','admin'],default:'citizen'}, subscribedZones:[String] },{timestamps:true}); export default mongoose.model('User',UserSchema);
