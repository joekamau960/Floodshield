import Asset from '../models/Asset.js'; import Alert from '../models/Alert.js'; const createAsset = async (req,res)=>{ const {assetId,name,lat,lon}=req.body; const a=new Asset({assetId,name,lat,lon,status:'Normal'}); await a.save(); res.json({ok:true,asset:a}); }; const createAlert = async (req,res)=>{ const {zone,riskLevel,message}=req.body; const a=new Alert({zone,riskLevel,message}); await a.save(); res.json({ok:true,alert:a}); }; export { createAsset, createAlert };
