fetch('/api/predict').then(r=>r.json()).then(data=>{const infra=data.infrastructureStatus;const ul=document.getElementById('admin-assets');ul.innerHTML='';Object.entries(infra).forEach(([k,a])=>{const li=document.createElement('li');li.innerHTML=`<strong>${k}</strong> - ${a.status}`;ul.appendChild(li);});const ctx=document.getElementById('sysChart').getContext('2d');new Chart(ctx,{type:'bar',data:{labels:Object.keys(infra),datasets:[{label:'Status',data:Object.values(infra).map(x=> x.status.includes('High')?2: x.status.includes('Warning')?1:0)]}});document.getElementById('btn-drill').addEventListener('click',()=>{ alert('Drill broadcast (demo)'); });document.getElementById('btn-add').addEventListener('click',()=>{ alert('Add asset - not implemented in demo'); });