async function postJson(url, body){ const r = await fetch(url, {method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify(body)}); return r.json(); } document.addEventListener('DOMContentLoaded', ()=>{ const loginBtn = document.getElementById('btn-login'); if (loginBtn){ loginBtn.addEventListener('click', async ()=>{ const email = document.getElementById('email').value; const password = document.getElementById('password').value; try{ const res = await postJson('/auth/login', { email, password }); if (res?.token){ localStorage.setItem('fs_token', res.token); alert('Logged in'); window.location = '/admin.html'; } else alert('Login failed'); }catch(e){ alert('Login failed (server)'); } }); } const registerBtn = document.getElementById('register-btn'); if (registerBtn){ registerBtn.addEventListener('click', async ()=>{ const name = document.getElementById('name').value; const email = document.getElementById('email').value; const phone = document.getElementById('phone').value; const password = document.getElementById('password').value; try{ const res = await postJson('/auth/register', { name, email, phone, password }); if (res?.ok){ alert('Registered - login'); window.location='/login.html'; } else alert('Register failed'); }catch(e){ alert('Register failed (server)'); } }); } });